import { Schema, Mixins } from "themer";

export const schema: Schema = {
    GLOBAL: {
        test: {
            $type: "color",
            $required: true
        }
    }
};

export const mixins: Mixins = {

};